# Discord Bot Integration

Payload-builder integration package for Discord notifications and callback action flows.

This package is executed in plugin runtime and returns normalized payloads that backend Discord transport sends.
