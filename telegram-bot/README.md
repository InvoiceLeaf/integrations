# Telegram Bot Integration

Payload-builder integration package for Telegram notifications and callback action flows.

This package is executed in plugin runtime and returns normalized payloads that backend Telegram transport sends.
