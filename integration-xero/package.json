{
  "name": "@invoiceleaf/integration-xero",
  "version": "1.0.0",
  "description": "Xero accounting integration for InvoiceLeaf scheduled invoice synchronization",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "keywords": [
    "invoiceleaf",
    "integration",
    "xero",
    "accounting",
    "sync"
  ],
  "author": "InvoiceLeaf <support@invoiceleaf.com>",
  "license": "MIT",
  "peerDependencies": {
    "@invoiceleaf/integration-sdk": "^1.7.0"
  },
  "devDependencies": {
    "@invoiceleaf/integration-sdk": "^1.7.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "files": [
    "dist",
    "manifest.json",
    "README.md"
  ]
}
