# Google Drive Integration

Import PDF invoices from Google Drive and upload InvoiceLeaf documents back to Drive.

## Features

- Scheduled Google Drive PDF crawl (`crawlPdfFiles`)
- On-demand PDF import action (`import-now`)
- Directory listing action (`list-directories`)
- Upload action for InvoiceLeaf documents (`upload-document`)
- OAuth connection diagnostics (`testConnection`)
